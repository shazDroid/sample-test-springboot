# springboot-sample

Minimal Spring Boot (Maven) sample with:
- 1 controller: `HelloController`
- 1 service: `GreetingService`

## Run

```bash
mvn spring-boot:run
```

Then open:
- `http://localhost:8080/api/hello`
- `http://localhost:8080/api/hello?name=Shahbaz`

## Test

```bash
mvn test
```
